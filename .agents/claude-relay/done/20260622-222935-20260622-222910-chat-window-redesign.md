请直接修改当前 E:\Codes\LMP 项目，目标是把页面底部/右侧的 AI 区域改成“真正的可滚动会话窗口”，而不是一个可折叠/可全屏的面板。

用户明确要求：
1. 取消“场景机理拆解”这部分的独立展示块，把释放出来的空间全部让给 AI 教学助教。
2. AI 教学助教要和整页视觉融合，不要像一个悬浮工具条或可伸缩抽屉。
3. 底部输入框始终存在；上面的会话内容区域要能滚动。
4. 在用户没有输入任何问题之前，把“被取消的机理拆解内容”作为会话的默认文本内容展示出来。

请按以下方式做：
- 不要重做整个框架，不动 old/。
- 优先修改 `sced_and_lmp_educational_system.html`，必要时少量改 `server.js`，但不要为了这个需求改后端架构。
- 需要删除/弱化现有的折叠、全屏、浮层式 AI 面板交互，让它变成页面内的稳定会话区。
- 会话区要像聊天记录：上方消息区滚动，底部输入区固定可见。
- 默认文本建议直接复用当前场景机理拆解的内容，作为第一条系统/assistant 消息渲染出来，而不是单独留一个“场景机理拆解”区块。
- 保留设置入口（Base URL / API Key / Model / Temperature），但不要喧宾夺主。
- 保留预设快捷问题，如果布局空间不够，可以改成更紧凑的按钮区或折叠到设置里。
- 保证 1440x900 一屏可用，1366x768 不要崩，390x844 不要横向溢出。

实现后请自己做最小验证：
- `node --check server.js`
- 提取 HTML 内联脚本做语法检查
- 启动服务并确认 `/health`
- 给出你实际改了什么、验证了什么、还有什么没验证

请只做这一个视觉/交互收口目标，不要顺手再扩功能。
